server:
  port: ${PORT:8082}
management:
  security:
    enabled: false
eureka:
  instance:
    hostname: ${vcap.application.uris[0]:localhost}
    statusPageUrl: http://${eureka.instance.hostname}/application/info
    healthCheckUrl: http://${eureka.instance.hostname}/application/health
    homePageUrl: http://${eureka.instance.hostname}/
  client:
    serviceUrl:
      defaultZone: ${vcap.services.eureka.credentials.uri:http://127.0.0.1:8761}/eureka/
spring:
  rabbitmq:
    host: ${cloudamqp.credentials.http_api_uri:localhost}
    port: 32774
    username: guest
    password: guest
  cloud:
    stream:
      bindings:
        mcdonalds:
          destination: mcdonalds
          binder: rabbit
    config:
      uri: ${rnm-config.cfapps.io:http://127.0.0.1:8888}
mcdonalds:
  order:
    chance: ${CHANCE:100}
