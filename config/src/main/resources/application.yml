server:
  port: ${PORT:8888}
management:
  security:
    enabled: false
eureka:
  instance:
    hostname: ${vcap.application.uris[0]:localhost}
    non-secure-port: 80
  client:
    serviceUrl:
      defaultZone: ${vcap.services.eureka.credentials.uri:http://127.0.0.1:8761}/eureka/
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Turbots/szechuan-config.git